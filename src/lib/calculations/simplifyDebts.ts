// Isolated debt simplification, independently testable (backend Sec 34).
export { calculateSettlements, simplifyDebts } from '../../utils/calculations';
export type { SimplifiedDebt } from '../../utils/calculations';
