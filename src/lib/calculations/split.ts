// Isolated split math, independently testable (backend Sec 34).
export {
  calculateEqualShares,
  calculatePercentageShares,
  calculateSharesSplit,
  distributeByWeights,
  getTotalShares,
} from '../../utils/calculations';
